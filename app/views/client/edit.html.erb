<div class="authform_large">
   <%= form_for(:client, url: client_path(@client.id),   :html => { :method => :put, :role => 'form', :multipart => true}) do |f| %>
    <div class="col-md-12">
    	<%= devise_error_messages! %>
        <% if @client.errors.any? %>
        <div id="error_explanation">
        <ul>
        <% @client.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
          <% end %>
        </ul>
        </div>
        <% end %>
       <h3>Edit a New Client</h3>
    </div>
  <div class="row">
    <div class="col-md-12">
          <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, :autofocus => true, class: 'form-control' %>
          </div>
           <div class="form-group">
            <%= f.label :description %>
            <%= f.text_area :description, rows: 8, class: 'form-control' %>
          </div>      
    </div>
    <div class="col-md-12">
      <%= f.submit 'Update Client', :class => 'btn btn-primary btn-lg btn-block' %>
    </div>
  </div>
  <% end %>
</div>
